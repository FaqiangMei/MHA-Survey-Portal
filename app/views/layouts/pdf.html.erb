<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Survey Response PDF</title>
  <style>
    /* Base typography */
    @page { margin: 1.2cm 1.2cm; }
    html, body { font-family: "Helvetica Neue", Helvetica, Arial, sans-serif; color: #24303a; font-size: 13px; }

    /* Header */
    .pdf-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px; }
    .pdf-brand { font-size: 18px; font-weight: 700; color: #1f6feb; }
    .pdf-meta { text-align: right; font-size: 12px; color: #586069; }

    /* Info box */
    .user-info { border: 1px solid #e6eef8; background: #fbfdff; padding: 10px 12px; border-radius: 6px; margin-bottom: 14px; display: flex; gap: 20px; }
    .user-info .col { min-width: 160px; }
    .user-info .label { font-size: 11px; color: #6b7785; margin-bottom: 4px; }
    .user-info .value { font-weight: 600; color: #102a43; }

    h1.survey-title { font-size: 20px; margin: 0 0 8px 0; color: #102a43; }
    h3.category-title { font-size: 14px; margin: 12px 0 6px 0; color: #134e4a; }

    .question-block { padding: 8px 10px; border-bottom: 1px solid #eef3f8; }
    .question-number { color: #0b5cff; font-weight: 700; margin-right: 6px; }
    .question-body { font-weight: 600; color: #0b2a3a; }
    .form-control, textarea { width: 100%; border: none; background: transparent; color: #102a43; }
  .multiple-choice-options { margin-top: 6px; }
  /* Multiple choice option styling */
  .mc-option { display: flex; align-items: center; gap: 8px; padding: 4px 0; }
  .mc-marker { width: 26px; display: inline-block; text-align: center; font-size: 16px; color: #0b5cff; }
  .mc-label { display: inline-block; color: #102a43; }
  .mc-option.selected { background: #eef8ff; padding: 6px 8px; border-radius: 4px; }

    /* Prevent breaking question block across pages */
    .question-block { page-break-inside: avoid; }

    /* Footer */
    .pdf-footer { position: fixed; bottom: 6px; left: 0; right: 0; text-align: center; font-size: 11px; color: #8b99a6; }

    /* Small helpers */
    .muted { color: #6b7785; }
  </style>
</head>
<body>
  <div class="pdf-header">
    <div class="pdf-brand">Health — Survey Response</div>
    <div class="pdf-meta">Generated: <%= Time.current.strftime('%Y-%m-%d %H:%M') %></div>
  </div>

  <%= yield %>

  <div class="pdf-footer">This document was generated from the Health application.</div>
</body>
</html>