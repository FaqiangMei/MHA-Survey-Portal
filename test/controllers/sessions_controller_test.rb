require "test_helper"

class SessionsControllerTest < ActionDispatch::IntegrationTest
  # Sessions are handled by Devise, not a custom SessionsController
  # Tests for authentication are in omniauth_callbacks_controller_test.rb
end
