<%# Render the same read-only survey response content used by the HTML show page so the PDF
   matches the "View response" output. The controller already sets @survey_response and
   @question_responses when rendering the show action. %>

<div class="survey-pdf">
  <%# Render the HTML partial explicitly so Rails doesn't look for a PDF-specific partial. %>
  <%= render partial: 'survey_responses/survey_response', formats: [:html], locals: { survey_response: @survey_response } %>
</div>